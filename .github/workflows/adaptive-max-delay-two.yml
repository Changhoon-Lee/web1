name: Adaptive Max-Normalized Delay-Two Lean Gate

on:
  push:
    branches:
      - collatz-k19-gate-clean-20260808
    paths:
      - collatz_k19/AdaptiveCriticalChoiceMax.lean
      - collatz_k19/AdaptiveSharpDelayTwo.lean
      - collatz_k19/AdaptiveCriticalChoiceMaxDelayTwo.lean
      - .github/workflows/adaptive-max-delay-two.yml
  workflow_dispatch:

permissions:
  contents: read

env:
  SOURCE_URL: https://www.proofatlas.ai/sources/collatz-predecessor-090/commits/5f76a170e74ea5b0c37c56683bd4c1e9d72e5e3f/collatz_predecessor_090-1.0.0.tar.gz
  SOURCE_ARCHIVE_SHA256: bc59cf3067a9377dbc0d4fc32d2eb4f228051f65cf2f41087a0066ef7ddedc9b

jobs:
  lean-proof:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Prepare pinned Lean project
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p source logs
          archive="$RUNNER_TEMP/source.tar.gz"
          curl --fail --location --retry 5 --retry-all-errors "$SOURCE_URL" -o "$archive"
          echo "$SOURCE_ARCHIVE_SHA256  $archive" | sha256sum --check -
          tar -xzf "$archive" -C source --strip-components=1
          cp collatz_k19/AdaptiveCriticalChoiceMax.lean source/
          cp collatz_k19/AdaptiveSharpDelayTwo.lean source/
          cp collatz_k19/AdaptiveCriticalChoiceMaxDelayTwo.lean source/
          sha256sum \
            collatz_k19/AdaptiveCriticalChoiceMax.lean \
            collatz_k19/AdaptiveSharpDelayTwo.lean \
            collatz_k19/AdaptiveCriticalChoiceMaxDelayTwo.lean \
            | tee logs/source.sha256

      - name: Install source-pinned Lean
        uses: leanprover/lean-action@v1
        with:
          lake-package-directory: source
          auto-config: false
          build: false
          test: false
          use-github-cache: false

      - name: Fetch pinned dependencies
        working-directory: source
        shell: bash
        run: |
          set -euxo pipefail
          lake update
          lake exe cache get
          lake build Erdos1135.KrasikovLagarias.AdaptiveCriticalChoice \
            2>&1 | tee ../logs/dependencies.out
          grep -q 'Build completed successfully' ../logs/dependencies.out

      - name: Compile imported strengthening modules
        working-directory: source
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p .lake/build/lib/lean
          lake env lean AdaptiveCriticalChoiceMax.lean \
            -o .lake/build/lib/lean/AdaptiveCriticalChoiceMax.olean \
            -i .lake/build/lib/lean/AdaptiveCriticalChoiceMax.ilean \
            2>&1 | tee ../logs/lean.out
          lake env lean AdaptiveSharpDelayTwo.lean \
            -o .lake/build/lib/lean/AdaptiveSharpDelayTwo.olean \
            -i .lake/build/lib/lean/AdaptiveSharpDelayTwo.ilean \
            2>&1 | tee -a ../logs/lean.out
          lake env lean AdaptiveCriticalChoiceMaxDelayTwo.lean \
            2>&1 | tee -a ../logs/lean.out
          if grep -q 'sorryAx' ../logs/lean.out; then exit 5; fi
          if grep -nE '(^|[^[:alnum:]_])(sorry|admit)([^[:alnum:]_]|$)' \
              AdaptiveCriticalChoiceMax.lean AdaptiveSharpDelayTwo.lean \
              AdaptiveCriticalChoiceMaxDelayTwo.lean; then
            exit 6
          fi
          grep -q \
            'AdaptiveCriticalChoiceMaxDelayTwo.exponential_lower_bound_max_delay_two' \
            ../logs/lean.out
          printf '%s\n' \
            'ADAPTIVE_MAX_NORMALIZATION=PASS' \
            'ADAPTIVE_EXACT_DELAY_TWO=PASS' \
            'ADAPTIVE_SHARP_CONSTANT_LAMBDA_NEG_TWO_OVER_MAX=PASS' \
            'NO_SORRYAX=PASS' \
            'FORMAL_GATE=PASS' \
            | tee ../logs/verdict.out

      - name: Upload Lean evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: adaptive-max-delay-two-${{ github.run_id }}
          path: logs/
          if-no-files-found: error
          retention-days: 30
